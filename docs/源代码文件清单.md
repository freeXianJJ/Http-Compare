# 源代码文件清单

请先运行 `create-dirs.bat` 创建目录结构，然后按照以下清单创建文件。

## 一、类型定义文件

### 1. src\types\api.ts

```typescript
export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
  PATCH = 'PATCH',
  HEAD = 'HEAD',
  OPTIONS = 'OPTIONS'
}

export enum ParamType {
  QUERY = 'query',
  PATH = 'path',
  BODY = 'body',
  FORM = 'form',
  HEADER = 'header'
}

export interface Parameter {
  id: string;
  name: string;
  type: ParamType;
  value: string;
  enabled: boolean;
  description?: string;
}

export interface ApiConfig {
  id: string;
  name: string;
  url: string;
  method: HttpMethod;
  parameters: Parameter[];
  headers: Parameter[];
  createdAt: number;
  updatedAt: number;
}
```

### 2. src\types\service.ts

```typescript
export enum Protocol {
  HTTP = 'http',
  HTTPS = 'https'
}

export interface ServiceConfig {
  protocol: Protocol;
  host: string;
  port: number;
  token: string;
  tokenPrefix: string;
  tokenHeader: string;
}

export interface ServicesConfig {
  oldService: ServiceConfig;
  newService: ServiceConfig;
}
```

### 3. src\types\diff.ts

```typescript
import { HttpMethod } from './api';
import { ApiConfig } from './api';

export interface RequestInfo {
  url: string;
  method: HttpMethod;
  headers: Record<string, string>;
  body?: any;
  timestamp: number;
  duration: number;
}

export interface ResponseInfo {
  status: number;
  statusText: string;
  headers: Record<string, string>;
  body: any;
  timestamp: number;
  error?: string;
}

export interface TestResult {
  id: string;
  apiConfig: ApiConfig;
  oldService: {
    request: RequestInfo;
    response: ResponseInfo;
  };
  newService: {
    request: RequestInfo;
    response: ResponseInfo;
  };
  diffResult: DiffResult;
  timestamp: number;
}

export interface DiffResult {
  identical: boolean;
  differences: DiffItem[];
  summary: {
    statusMatch: boolean;
    headerMatch: boolean;
    bodyMatch: boolean;
    totalDiffs: number;
  };
}

export interface DiffItem {
  path: string;
  type: 'added' | 'deleted' | 'modified' | 'type-mismatch';
  oldValue?: any;
  newValue?: any;
  oldType?: string;
  newType?: string;
}
```

---

**注意**：由于文件数量较多，完整的源代码将分多个文档提供。

继续查看下一部分：
- 状态管理文件 (stores)
- 服务层文件 (services)
- 组件文件 (components)
- 工具函数 (utils)
- 样式文件 (styles)
- 入口文件

建议您：
1. 先运行 `create-dirs.bat` 创建目录
2. 然后我将为每个文件单独创建
3. 或者您可以使用 Git 或其他方式获取完整代码

需要我继续创建下一批源代码文件吗？
